apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: dockernetworks.guardian.dev
  labels:
    type: network
spec:
  scope: Namespaced
  group: guardian.dev
  names:
    kind: DockerNetwork
    plural: dockernetworks
    singular: dockernetwork
    shortNames:
      - dnet
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          description: |
            The vyos router
          x-kubernetes-preserve-unknown-fields: true
          properties: {}
